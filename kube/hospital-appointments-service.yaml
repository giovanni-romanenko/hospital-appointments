apiVersion: v1
kind: Service
metadata:
  name: hospital-appointments
spec:
  type: NodePort
  selector:
    app: hospital-appointments
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
